/*
 * Mission Story — horizontal story timeline (spec, Section 4):
 * Vision → Beginning → Growth → Olympic Dream → Future.
 * Stubbed in Phase 1 so the page structure matches the INDEX;
 * cinematic milestone content builds out alongside Phase 4 motion.
 */
export default function Mission() {
  return (
    <section className="section" id="about">
      <div className="section-inner">
        <p className="eyebrow">Our Story</p>
        <h2 className="section-title">The Mission</h2>
        <span className="stub-note">
          Story timeline — Vision · Beginning · Growth · Olympic Dream · Future
        </span>
      </div>
    </section>
  )
}
